pluginGroup = cx.akash.blackbox
pluginName = blackbox
pluginVersion = 0.1.0

# Min IDE build (2024.2 = 242)
pluginSinceBuild = 242
pluginUntilBuild =

# Target IDE for runIde / verifyPlugin (IC = IntelliJ IDEA Community)
platformType = IC
platformVersion = 2024.2.4

# Extra plugins required at runtime (none mandatory for IPC-only usage).
# JetBrains MCP Server is wired as an *optional* dependency via plugin.xml.
platformPlugins =
platformBundledPlugins =

# Java / Kotlin
javaVersion = 17
kotlinVersion = 1.9.24

# Gradle
org.gradle.jvmargs = -Xmx2g -XX:+UseG1GC -XX:MaxMetaspaceSize=512m
org.gradle.caching = true
org.gradle.configuration-cache = false
kotlin.stdlib.default.dependency = false
